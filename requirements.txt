# Comentários começam com o símbolo de cerquilha (#)
# Este é um exemplo de arquivo requirements.txt

# Dependências principais
requests==2.31.0
numpy==1.22.0
pandas==1.3.3
pytest==6.2.5  # Dependência do pytest

# Dependências adicionais
beautifulsoup4==4.10.0
matplotlib==3.4.3

# Caminho do executável pytest (substitua pelo caminho correto no seu sistema)
# Certifique-se de que o pytest esteja instalado no ambiente virtual
# Exemplo: /caminho/para/ambiente_virtual/bin/pytest
